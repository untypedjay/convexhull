public class ConvexPolygon {
    // checks if point p is inside of the convex polygon
    public boolean containsPoint(Point p) {
        return false;
    }

    // returns the i-th corner point of the convex polygon
    public Point get(int i) {
        return null;
    }

    // returns the number of corner points of the convex polygon
    public int size() {
        return 0;
    }

    // constructs a string in the format: [(x1,y1), ..., (xn,yn)].
    public String toString() {
        return "test";
    }
}
